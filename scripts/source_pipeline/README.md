# Source Pipeline Scripts

These scripts document the original step-wise experimental pipeline used to build candidate-level datasets, train/evaluate the digital twin, run shielding variants, and aggregate robustness results.

They are included for transparency. Some scripts require the full processed candidate-level dataset, which is not included in this lightweight public package. The paper-level result tables and figure data are provided under `results/` for direct verification of reported tables and figures.
